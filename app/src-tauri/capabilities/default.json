{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for AnyLive TTS",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:default",
    "core:tray:default"
  ]
}
